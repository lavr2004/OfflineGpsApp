<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:mapsui="clr-namespace:Mapsui.UI.Maui;assembly=Mapsui.UI.Maui"
             x:Class="OfflineGpsApp.MainPage">
    <Grid>
        <mapsui:MapView x:Name="MapViewXaml" VerticalOptions="FillAndExpand"/>
        <Button Text="Загрузить GPX" Clicked="OnLoadGpxClicked" VerticalOptions="End"/>
        <!-- ADDED: progress bar and label for filtering progress -->
        <!--<StackLayout x:Name="ProgressBarStackLayout" VerticalOptions="Center" HorizontalOptions="Center" IsVisible="{Binding IsFiltering, Mode=OneWay}">
            <Label x:Name="ProgressLabel" Text="Filtering monuments: 0%" FontSize="16" TextColor="White" BackgroundColor="Black" Padding="10" />
            <ProgressBar x:Name="ProgressBar" Progress="0" WidthRequest="200" />
        </StackLayout>-->
    </Grid>
</ContentPage>